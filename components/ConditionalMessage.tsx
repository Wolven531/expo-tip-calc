import React, { FC, memo, useState } from 'react'
import {
	StyleSheet,
	Text,
	TouchableOpacity,
	View
} from 'react-native'

const ConditionalMessage: FC<any> = (props) => {
	return ( null )
}

export { ConditionalMessage }
